{
  "name": "sticky-session",
  "description": "Sticky session balancer based on a `cluster` module. Updated for PandoraPay",
  "version": "1.1.2",
  "author": "Budisteanu Ionut Alexandru",
  "main": "./lib/sticky-session",
  "dependencies": {
    "debug": "^4.1.1",
    "ip": "^1.1.5"
  },
  "engines": {
    "node": ">= 0.12.0"
  },
  "devDependencies": {
    "jscs": "^2.1.1",
    "jshint": "^2.8.0"
  },
  "license": "MIT",
  "scripts": {
    "lint": "jscs lib/*.js lib/**/*.js && jshint lib/*.js lib/**/*.js",
    "test": "npm run lint && (echo test/*-test.js | xargs node)"
  },
  "optionalDependencies": {}
}
